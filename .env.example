# Copy to .env in the repository root. Never commit the real key.
OPENAI_API_KEY=your_openai_api_key_here

# Optional model overrides. Defaults preserve the approved GPT-5.6 setup.
OPENAI_MODEL=gpt-5.6
# OPENAI_DECISION_1_MODEL=gpt-5.6
# OPENAI_DECISION_2_MODEL=gpt-5.6

# Optional local study output directory.
# NEUROSCAPE_RESULTS_DIR=study-results
